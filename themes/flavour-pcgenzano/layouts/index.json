[{{ $first := true }}{{ range where .Site.AllPages "Kind" "ne" "home" }}{{ if and .Title (not (eq .Title "")) }}{{ if $first }}{{ $first = false }}{{ else }},{{ end }}{"title":{{ .Title | jsonify }},"url":{{ .Permalink | jsonify }},"content":{{ .Plain | truncate 500 | jsonify }},"section":{{ .Section | jsonify }}}{{ end }}{{ end }}]
