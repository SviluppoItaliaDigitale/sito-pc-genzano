{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- $prodBase := .Site.Params.productionBaseURL | default .Site.BaseURL -}}
  {{- $basePath := (urls.Parse .Site.BaseURL).Path | default "/" -}}
  {{- range .Data.Pages -}}
  {{- if not .Params.noindex }}
  {{- $pagePath := strings.TrimPrefix $basePath .RelPermalink -}}
  <url>
    <loc>{{ printf "%s/%s" (strings.TrimRight "/" $prodBase) $pagePath | safeHTML }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05-07:00" | safeHTML }}</lastmod>
    {{- end }}
    {{- with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>
    {{- end }}
    {{- if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>
    {{- end }}
  </url>
  {{- end }}
  {{- end }}
  {{/* URL statiche extra (giochi, app accessibilità, formazione PC, quiz)
       dichiarate in data/sitemap_extra.yaml — mantenere allineato quando si
       aggiungono nuovi giochi/app statiche con robots index,follow. */}}
  {{- with .Site.Data.sitemap_extra }}
  {{- range .urls }}
  <url>
    <loc>{{ printf "%s/%s" (strings.TrimRight "/" $prodBase) (strings.TrimPrefix "/" .path) | safeHTML }}</loc>
    {{- with .changefreq }}
    <changefreq>{{ . }}</changefreq>
    {{- end }}
    {{- with .priority }}
    <priority>{{ . }}</priority>
    {{- end }}
  </url>
  {{- end }}
  {{- end }}
</urlset>
