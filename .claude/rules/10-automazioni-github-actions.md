# Automazioni periodiche — GitHub Actions

Tutti i workflow di manutenzione girano **ogni lunedì** (primo giorno della settimana), scaglionati in orari diversi per non caricare il runner nello stesso momento. L'utente ha scelto il lunedì per avere una finestra settimanale costante di issue/verifica da affrontare.

🔒 **Hardening trasversale (15/08/2026):** (a) **ogni job ha `timeout-minutes` esplicito** — un run appeso diventa *failure* entro il timeout e quindi visibile al watchdog 🩺 (che scansiona solo i run falliti, non quelli in corso da ore); mantenere il timeout su ogni nuovo workflow; (b) **le action di terze parti sono pinnate a commit SHA** (`uses: owner/action@<sha> # vX.Y.Z`) perché i tag sono mutabili e quelle action maneggiano segreti (FTP Aruba, Telegram, Gemini) — per aggiornarle si sostituisce lo SHA e il commento versione (le `actions/*` ufficiali restano su tag). Nuove action di terzi vanno pinnate allo SHA fin dall'inizio; **(c) Dependabot** (`.github/dependabot.yml`, ecosistema `github-actions`, lunedì 06:30 Europe/Rome, PR settimanale cumulativa con label `manutenzione`+`automazione`) mantiene freschi i pin aggiornando SHA + commento versione — le sue PR passano dai gate standard e **il merge resta umano**.

## Lavorazione autonoma delle issue automatiche (Routine CCR, 11 luglio 2026)

🟢 **Istruzione permanente dell'utente (11/07/2026):** le issue aperte dai workflow (`github-actions`) NON restano in attesa di lettura umana — l'obiettivo è **portare e mantenere a ZERO il backlog**. Una **Routine Claude Code Remote** (trigger `trig_01EL97B5vF55j3JAWGiNiNCM`, cron giornaliero 12:00 UTC, sessione fresca a ogni run, notifiche push+email all'utente) esegue ogni giorno:

- triage delle issue aperte di github-actions (mai quelle create da umani o con label `tracking`/`enhancement`/`bug`/`pinned`/`urgente-permanente`);
- **dedup** dei report settimanali della stessa famiglia (si lavora il più recente, i vecchi si chiudono come superati);
- **Categoria A — manutenzione e integrazioni pre-autorizzate** (link rotti, documenti, refusi, archiviazioni scadenze, glossario, Linee Guida IRC, video LIS/approfondimenti, freschezza, segnalazioni rientrate): lavorazione **fino a live** (PR + merge + verifica deploy) e chiusura issue col commit. Tutti i gate del repo restano vigenti (build, AGID, pertinenza, `image:`, no inventario);
- **Categoria B — pubblicazione di CONTENUTI NUOVI o scelte editoriali sostanziali** (nuovi articoli, versioni facili A2, riscritture rilevanti, cambi strutturali): il lavoro viene **preparato completo su branch con PR pronta ma NON mergiata** — l'utente dà l'OK finale ("pubblica la PR #N" o merge da GitHub); il run successivo chiude l'issue quando trova la PR mergiata;
- decisioni non delegabili (credenziali, spese, enti terzi): solo commento con raccomandazione;
- riepilogo conciso via notifica: chiuse / PR in attesa di OK / rimaste.

**Nasce** perché i video LIS segnalati dall'issue #601 del 22/06 sono stati scoperti "per caso" tre settimane dopo. Le sessioni interattive restano libere di lavorare le issue quando le incontrano (questa regola aggiunge il ramo automatico, non lo sostituisce).

## Routine "Cura schede didattiche scuole" (Routine CCR, 1 agosto 2026)

🟢 **Routine settimanale dedicata alle schede didattiche stampabili** (trigger `trig_01RESwx5eFP4SuTpF1iCFY4j`, cron lunedì 05:00 UTC ≈ 07:00 italiane, sessione fresca a ogni run, notifiche push+email). Richiesta esplicita dell'utente del 01/08/2026: *"un agente che settimanalmente deve assolutamente fare ciò che ti ho chiesto: aggiornare, sistemare, ampliare le schede didattiche in maniera impeccabile"*. Mandato in 8 punti:

1. **Simmetria kit ↔ schede** (rule 02 § "Coerenza kit didattici"): nessuna scheda orfana, nessun link a schede inesistenti.
2. **Refusi** (`scripts/check-refusi.py` sul perimetro schede).
3. **Vigenza normativa** delle citazioni nelle schede — con **divieto assoluto di inventare estremi normativi** (NO INVENZIONI; verifica su Normattiva/fonti primarie, in dubbio formulazione prudente).
4. **Qualità strutturale**: soluzioni degli esercizi col pattern `.soluzione-capovolta` (mai in chiaro sullo stesso foglio), banda affiliazioni in stampa su ogni scheda (vincolo ESC/E10435833, Reg. UE 2021/888), accessibilità WCAG (h1, alt, lang).
5. **Freschezza dei dati** citati (livelli di allerta, dati climatici, riferimenti a bollettini).
6. **Ampliamento mirato**: max 1-2 schede nuove a settimana e solo su buchi reali della matrice fasce × temi.
7. **Rigenerazione pacchetti ZIP** (`scripts/genera-pacchetti-kit.py` — aggiorna da solo anche la dimensione dichiarata nei kit).
8. **Pubblicazione con build pulita** fino a live; se non c'è nulla da fare **lo dichiara senza inventare lavoro**.

Gestione: https://claude.ai/code/routines/trig_01RESwx5eFP4SuTpF1iCFY4j. Le sessioni interattive restano libere di lavorare sulle schede; la routine è la rete di manutenzione continua.

## Routine "Cura kit calamità categorie vulnerabili" (Routine CCR, 1 agosto 2026)

🟢 **Routine settimanale dedicata ai Kit Calamità** (trigger `trig_013VgcKgHzKYkUrJeeDP7EX5`, cron martedì 05:00 UTC ≈ 07:00 italiane — giorno diverso dalla routine schede per distribuire il carico —, sessione fresca a ogni run, notifiche push+email). Richiesta esplicita dell'utente del 01/08/2026: *"la stessa cosa va fatta con i vari Kit: aggiornati, modificati, ampliati, includendo anche un nuovo agente che li controlli nelle loro normative"*. Perimetro: pagine `content/formazione/kit-calamita*` + schede A4 `static/formazione/kit-calamita-*/` (12 categorie). Mandato in 8 punti, speculare a quello delle schede ma con **priorità alla vigenza normativa**:

1. **Vigenza normativa (priorità)**: censimento e verifica su fonti primarie di norme (D.Lgs./L./D.M./DPCM) e standard internazionali (NCTSN PFA, IFRC, WHO, Sphere, ISO/UNI) citati nei kit — con divieto assoluto di inventare estremi normativi (NO INVENZIONI).
2. **Simmetria hub ↔ kit ↔ schede**: nessuna scheda orfana, nessun link a materiali inesistenti.
3. **Refusi** + coerenza numeri di emergenza (112 / 803 555 / 1530).
4. **Qualità strutturale**: banda affiliazioni in stampa (ESC + E10435833, `.scheda::after` di `kit-calamita-shared/print.css`), soluzioni giochi capovolte, template `.carta-id-*`, WCAG di base, attribuzione ARASAAC.
5. **Freschezza dei dati** (recapiti, edizioni, link; contenuti sanitari solo da fonti istituzionali).
6. **Ampliamento mirato**: max 1-2 materiali/settimana solo su buchi reali della matrice categorie × materiali. 🔴 I materiali **nuovi** seguono la **Categoria B** (§ "Lavorazione autonoma delle issue automatiche"): PR preparata completa ma **NON mergiata** — trattandosi di indicazioni sanitarie/per persone vulnerabili, il merge attende l'OK esplicito dell'utente.
7. **Coerenza ecosistema**: hub, mappa-sito, assistente (sotto-albero `kc_*`), pacchetti.
8. **Pubblicazione con build pulita**: le correzioni di **manutenzione** (Categoria A) vanno fino a live; gli **ampliamenti** restano in PR come da punto 6. Se non c'è nulla da fare lo dichiara.

Gestione: https://claude.ai/code/routines/trig_013VgcKgHzKYkUrJeeDP7EX5. **Prompt rafforzato il 06/09/2026** (gate `pc-didattica-reviewer` + `pc-fact-checker` + `pc-revisore-scientifico` a rotazione sul catalogo; nessun divieto assoluto senza scenario; schede sanitarie «da validare» finché non firmate). La routine delle schede (`trig_01RESwx5eFP4SuTpF1iCFY4j`) è stata creata dal pannello web e **non è aggiornabile da una sessione**: il prompt aggiornato è in `riferimenti-interni/routine-prompts/cura-schede-didattiche.md`, da incollare a mano.

## Routine "Audit interno completo del sito" (Routine CCR, 6 settembre 2026)

🔴 **Routine mensile** (trigger `trig_01RMQwDs5Ku2mRfkwkDZnKmx`, cron il **3 del mese alle 04:00 UTC ≈ 06:00 italiane**, sessione fresca, notifiche push+email). Nasce dall'istruzione permanente dell'utente del 06/09/2026: *«queste cose non devono mai più capitare: non devo più rivolgermi ad altre intelligenze artificiali per fare un audit e poi passartelo»*, dopo un audit esterno con 24 rilievi (11 P1) tutti fondati. La routine invoca l'agente **`pc-audit-completo`** con perimetro completo:

1. **Controlli deterministici**: build, `check-integrita-asset.py`, `check-ancore.py`, `check-parita-schede.py`, `check-dati-schede.py`, `check-jsonld.py`, `check-refusi.py`, `audit-grammatica-italiana.py`, `check-freshness.py`, `check-articoli-programmati.py`, `check-fonti-cruscotto.py`, `genera-chrome-menu.py --check`, rigenerazione pacchetti, `verifica-fingerprint-live.sh`, `smoke-test-live.sh`.
2. **Specialisti**: `pc-fact-checker`, `pc-didattica-reviewer`, `pc-conformita-legale`, `pc-integrita-tecnica`, `pc-coerenza-trasversale`, `pc-revisore-scientifico`, `pc-desk-giornalistico`, `pc-revisore-codice`, `pc-revisore-automazioni`, `pc-sicurezza`, `pc-revisore-traduzioni`, `pc-dati-e-feed`, `pc-esercitazione-emergenza`, `pc-verifica-visiva`, `pc-usabilita`, `pc-documentazione`, `pc-accessibility-auditor` (+ normative, freshness, SEO, site-auditor dove serve).
3. **Rapporto** in formato rilievi (prova, impatto, correzione, verifica, P1/P2/P3) salvato in `riferimenti-interni/audit-interni/AAAA-MM-DD-audit-interno.md`.
4. **Chiusura**: Categoria A fino a live (un merge per volta), Categoria B in PR non mergiata, validazioni esterne come issue `audit` + `revisione` con responsabile.

Le sessioni interattive possono invocare `pc-audit-completo` anche in modalità **mirata** (una sezione, prima di una distribuzione). Un mese senza rilievi si dichiara come tale.

## Modello di priorità del deploy (31 maggio 2026)

🔴 I deploy seguono **3 livelli di priorità** per dare precedenza a sicurezza e contenuti rispetto agli aggiornamenti di sfondo (frequenti). `deploy.yml` ha l'input `workflow_dispatch.inputs.priority` (default `urgent`) e una `concurrency` condizionale: `cancel-in-progress: ${{ github.event_name != 'workflow_dispatch' || github.event.inputs.priority != 'background' }}`. Significato: i deploy di contenuto (push merge PR) e i dispatch non-`background` (allerta, manuale) **preemptano** un deploy in corso; il deploy `background` (coalescer) **cede sempre**. Ogni deploy fa `checkout` di HEAD, quindi un deploy preemptato non perde nulla: il successivo ricarica tutto.

| Livello | Trigger | Comportamento |
|---|---|---|
| 🔴 **Sicurezza** | `check-allerta.yml`, `pubblica-programmata.yml` → `gh workflow run deploy.yml -f priority=urgent` | **Immediato**, preempta. L'allerta meteo resta reattiva (~15 sec), mai coalescata. |
| 🟦 **Contenuti** | merge PR articoli (evento `push`) | **Immediato**, preempta i deploy di sfondo. |
| ⬜ **Sfondo** | `deploy-coalescer.yml` (`workflow_run` sui workflow meteo + cron `13,43 * * * *` come fallback) → `-f priority=background` | **Un solo deploy** che raccoglie tutti i commit dati non urgenti, lanciato appena un workflow di sfondo completa. Cede ad allerta/contenuti. |

### 🔴 Un merge per volta: non incatenare i merge (31/08/2026)

**Dopo un merge su `main`, aspetta che `deploy.yml` finisca prima di mergiare il successivo.** Se hai più modifiche pronte, **raggruppale in una sola PR** invece di mergiarne una dietro l'altra.

Il motivo è la preemption descritta qui sopra: ogni merge di contenuto **annulla il deploy in corso**. Se i merge si susseguono a distanza di pochi minuti, ogni run viene ucciso **a metà del caricamento FTP**, che è incrementale e dura 15-20 minuti. Il risultato è un sito servito a pezzi: pagine di build diverse contemporaneamente, e pagine ferme a una versione **precedente alla correzione** che credevi pubblicata.

⚠️ Sfumatura importante rispetto alla frase «un deploy preemptato non perde nulla» qui sopra: è vero che il deploy successivo **ricostruisce** tutto da HEAD, ma l'**upload FTP** riparte dal confronto coi file già sul server e non è istantaneo. Finché un deploy non arriva in fondo senza essere interrotto, lo stato live resta misto.

**Verifica prima di dichiarare che una modifica è live** (l'impronta di build è in ogni pagina):

```bash
curl -sSL "https://www.protezionecivilegenzano.it/<pagina>/?cb=$RANDOM" \
  | grep -o 'pc-build-sha content=[^ >]*'
# deve corrispondere al commit corrente di main; se pagine diverse danno SHA
# diversi, il deploy non è ancora arrivato in fondo: aspetta, non forzare.
```

**Storia:** il 31/08/2026 cinque PR mergiate in venti minuti hanno prodotto tre deploy annullati di fila (run 4307, 4308, 4309). Il sito è rimasto con quattro build diverse in linea e `/risorse-pronte/` ferma a due ore prima, con ancora visibile il testo che la PR aveva corretto. Nessun intervento è servito: è bastato **smettere di mergiare** e lasciare che un deploy chiudesse. Prima di applicare il rimedio dei file stantii (rule 05), verifica sempre che non sia semplicemente questo.

I workflow di **sfondo committano soltanto** (niente più `gh workflow run deploy.yml`): cartine meteo (`meteo-lazio`, `meteo-cartine-extra`), `ecmwf-charts`, `clima-castelli`, `aggiorna-video-correlati`, `aggiorna-stato-sistema`, `genera-qr-articoli`, `genera-pacchetti-kit` (QR/braille/pacchetti sono comunque rigenerati inline da `deploy.yml`; l'indice Pagefind dal 15/07/2026 è SOLO un artefatto di build generato da `deploy.yml`, mai committato). Il loro deploy lo fa il coalescer. Eccezioni che restano a deploy immediato `background` (deployano subito ma non preemptano): `scarica-foto-automatica` (cover banner, no rischio banner rotto) e `comprimi-podcast` (manuale). **Nasce 31/05/2026** dopo deploy-storm + articoli in coda dietro ai deploy meteo.

| Workflow | Frequenza | Scopo |
|---|---|---|
| `deploy.yml` | Ogni push su `main` | Build Hugo + deploy Aruba (FTP) + GitHub Pages. Prima della build esegue **pipeline asset idempotenti** (saltano i file già presenti): (a) `scripts/genera-qr-articoli.py` — QR `<slug>.png+.svg` in `static/qr/` per ogni nuovo articolo (dipendenza `segno` installata via pip); (b) `scripts/genera-braille.py` — versioni `.brf` (BRF/ICEB) in `static/braille/comunicazioni/` (dipendenza `liblouis`); (c) `scripts/auto-cover-mancanti.py` — cover tipografica col titolo + popolamento `image:`/`image_alt:` per articoli con `image: ""` o file mancante, **mai** sovrascrive foto utente (dipendenze `pillow` + `fonts-liberation`). Rete di sicurezza per le pubblicazioni da mobile che saltano lo step locale (storico: 16/05/2026 articolo Giro Formia senza bottone "Scarica QR"; 04/06/2026 articolo "Tromba d'aria a Roma" uscito col banner SVG di default perché creato da altra sessione con `image: ""` — da qui lo step cover pre-build, fail-safe race-free rispetto a `scarica-foto-automatica.yml` che gira DOPO `deploy.yml`). **Priorità (31/05/2026):** input `priority` + `concurrency` condizionale (vedi § "Modello di priorità del deploy"). |
| `deploy-coalescer.yml` | **`workflow_run`** al completamento dei workflow di sfondo frequenti (`meteo-lazio`, `meteo-cartine-extra`, `ecmwf-charts`) + cron `13,43 * * * *` (fallback) + `workflow_dispatch` | **Coalescer dei deploy di sfondo**: confronta `HEAD` di `main` con l'`headSha` dell'ultimo run di `deploy.yml`; se ci sono commit non deployati, lancia **un solo** `deploy.yml -f priority=background`. Raggruppa i numerosi commit automatici (meteo orario, ECMWF/cartine 4×/giorno, clima, video, stato, QR, pacchetti, indice) in un deploy periodico invece di uno per commit. Allerta e contenuti restano immediati e prioritari. **Trigger `workflow_run` aggiunto l'11/06/2026**: lo scheduler GitHub aveva saltato 6 slot cron consecutivi lasciando la cartina meteo live stantia per 5 ore; `workflow_run` non subisce i ritardi dello scheduler e la catena resta ancorata al trigger esterno cron-job.org che lancia i workflow meteo. Il check HEAD-vs-ultimo-deploy evita deploy ridondanti; `concurrency` group dedupe i run accodati. Permessi: `actions: write`. |
| `check-allerta.yml` | **Doppio trigger fail-safe**: (1) cron-job.org ogni 5 min via `workflow_dispatch` API GitHub [PRIMARIO, latenza ~15 sec]; (2) GitHub schedule `17 * * * *` [FAIL-SAFE orario al minuto 17, fuori dai picchi] | A ogni run esegue **tre script in pipeline** (refactoring 13 maggio 2026): (a) `scripts/check-allerta.py` — criticità idrogeologica/idraulica (CSV opendatasicilia `bollettino-{oggi,domani}-comuni-latest.csv` con filtro validità temporale + MAX time-aware, **fallback PDF Regione Lazio** se entrambi i CSV non rispondono, fuso `Europe/Rome` dinamico CET/CEST, blocco `domani` come pre-allerta solo se data > today); (b) `scripts/check-avvisi-meteo.py` — avvisi meteo avversi vento/neve/calore/gelate/mareggiate (parser PDF pagina `/bollettini/allertamenti` Regione Lazio, gestione avvisi compositi tipo "VENTO,NEVE,IDROGEOLOGICA"→`tipo='vento, neve'`, lookback 5gg, cessazione automatica alla scadenza); (c) `scripts/check-rischi-incendi.py` — rischio incendi AIB Zona 9 = Genzano (parser PDF pagina `/bollettini/rischi-incendi` Regione Lazio, mapping BASSO/MEDIO/MODERATO/ELEVATO → verde/gialla/arancione/rossa, attivo solo in campagna AIB giugno-ottobre). 🔴 **Scadenza per data (fix 2 giugno 2026):** se il bollettino di oggi non è ancora pubblicato all'alba e si scarica quello di ieri, lo script **promuove la previsione "domani" (= oggi reale)** invece di ripresentare il bollettino di ieri; se il bollettino è troppo vecchio per coprire oggi, **cessa il blocco** `rischio_incendi`. Difesa speculare lato template: `layouts/index.html` mostra la barra solo se `.data >= oggi` (build) + guard JS `data-valido` che la nasconde nel browser se la data è passata (anche con HTML statico non rigenerato). Nasce perché il 2 giugno la barra del 1° giugno restava visibile in homepage. Commit unico combinato se almeno uno dei tre ha modificato `data/allerta.json`. Anti-spam interno per ciascun blocco (livello / `domani` / `avviso_meteo` / `rischio_incendi`) + soglia stale_check 5h45min. Dipendenza runtime: `poppler-utils` per `pdftotext`. Latenza end-to-end ~15 sec al cambio livello DPC. Dal 10/06/2026: `concurrency` dedicata (group `check-allerta`, `cancel-in-progress: false` — i run si accodano, mai uccisi) e `timeout-minutes: 15`. **Al termine**, se `data/allerta.json` è cambiato, triggera esplicitamente `notifica-telegram.yml` via `gh workflow run` — i push fatti dal `GITHUB_TOKEN` non auto-triggerano i workflow su evento `push`, quindi senza questa chiamata il messaggio Telegram al cambio allerta non partiva (fix 14 maggio 2026). |
| `aggiorna-dati-sala.yml` | Ogni 15 min (cron `3,18,33,48 * * * *`) + `workflow_dispatch` | **Snapshot Sala situazioni**: rigenera `static/open-data/{notizie-agenzie,gdacs-eventi,ems-attivazioni}.json` (script fail-safe: fonte giù = file invariato, exit 0) e committa `[skip-dati-sala]` solo se i dati cambiano. La Sala situazioni li legge **prima da raw.githubusercontent.com** (CORS aperto, già in CSP, disponibili al commit senza aspettare il deploy) con fallback sulla copia del sito → freschezza effettiva **~15-20 min**. Vedi § "Snapshot Copernicus EMS" per il pattern. |
| `controllo-scadenza-pat.yml` | Mensile (1° del mese 09:00 UTC) | Verifica scadenza del PAT GitHub usato da cron-job.org. Se ≤30 giorni, apre/aggiorna issue con label `manutenzione` (`urgente` se ≤7 giorni). Procedura di rinnovo step-by-step nella issue. Richiede secret `CRONJOB_GH_PAT` configurato (stesso valore del token usato su cron-job.org). |
| `backup-documenti-aruba.yml` | Mensile (1° del mese 04:00 UTC) | **Backup di continuità** dei ~29 documenti istituzionali (incluso il **Piano di Emergenza Comunale**) che il sito serve da URL assoluti `www.protezionecivilegenzano.it` ma che NON hanno copia nel repo git — vivono solo sul server Aruba (`/area-download/normativa/`, `/documenti/download/`). `scripts/backup-documenti-aruba.py` deriva la lista dai contenuti (link assoluti a file senza copia in `static/`), li scarica, verifica (HTTP 200 + header `%PDF`/`PK` + non vuoto), genera `MANIFEST.md` con SHA-256. Il workflow li pubblica come asset della Release rolling `backup-documenti-aruba` (con `--clobber`: i file falliti mantengono la copia del mese prima). Apre issue se almeno un file fallisce. Nasce dopo l'audit del 14 maggio 2026 — quei file non erano coperti da nulla, `controllo-documenti.py` controlla solo i file dentro al repo. Recupero: scaricare l'asset dalla Release e ricaricarlo su Aruba. |
| `controllo-documenti.yml` | Lunedì | **Controllo settimanale dei documenti dentro al repo** (`scripts/controllo-documenti.py`): verifica che i PDF/allegati committati e linkati dai contenuti esistano e non siano vuoti/corrotti; apre issue (label `documenti`) se trova problemi. Complementare a `backup-documenti-aruba.yml` (che copre i file SOLO sul server Aruba). _(Riga aggiunta il 10/06/2026: il workflow esisteva ma mancava da questa tabella.)_ |
| `aggiorna-stato-sistema.yml` | Giornaliero 05:30 UTC | Aggiorna `data/stato-sistema.json` con lo stato dei 5 workflow critici (deploy, check-allerta, audit-sito, smoke-test, lighthouse) letto via `gh run list`, mappato in stato sintetico `success`/`failure`/`in_progress`. La pagina pubblica `/stato-sistema/` (idea #25 roadmap) lo legge al build per il cruscotto di trasparenza tecnica con semaforo. Commit `[skip-stato]` se cambiato. **Deploy coalescato** (`deploy-coalescer.yml`), non più ri-trigger diretto. **Dal 10/06/2026 include i controlli di salute giornalieri** (step "🩺"): (1) **watchdog universale** — scan dei run falliti nelle ultime 24h su TUTTI i workflow (copre il punto cieco di `notifica-ci-fallita.yml`, che monitora in tempo reale solo 4 workflow critici: 5 workflow del lunedì sono rimasti rossi per settimane senza notifica); (2) **dead-man check** della catena cron-job.org→check-allerta (<10 run/2h = degrado latenza allerta); (3) **scadenza certificato SSL** del dominio (soglia 21 giorni); (4) **crescita repo** (soglia 3 GB → pianificare migrazione podcast). Esito in **issue unica in-place** `🩺 Salute del sistema` (label `automazione`+`salute-sistema`) che si chiude da sola quando tutto rientra. |
| `pubblica-programmata.yml` | Giornaliero 06:00 UTC | Rilancia il deploy ogni mattina: gli articoli `draft: false` con `date` futura entrano nel sito al passaggio del giorno (Hugo li escludeva finché la data era oltre `now()`). NB: `draft: true` non viene flippato — gli articoli devono essere `draft: false` (regola: niente articoli in revisione) |
| `lighthouse-audit.yml` | 2×/giorno (cron `19 6,18 * * *`) | Audit performance/accessibilità/SEO su 3 URL live. **Dal 10/06/2026 schedulato** (prima girava dopo ogni deploy: ~20-40 run/giorno coi deploy meteo coalescati, senza informazione nuova; la verifica di propagazione di ogni deploy resta a `smoke-test-post-deploy.yml`) |
| `smoke-test-post-deploy.yml` | Post-deploy | Verifica live: 20 pagine principali rispondono 200, 7 traduzioni accessibili, mini-app statiche, marker chiave su 7 pagine, 2 header sicurezza. Apre issue urgente se trova regressioni. Logica in `scripts/smoke-test-live.sh` (riusabile in locale) |
| `verifica-deploy-aruba.yml` | `workflow_run` (completed, success) su "🚀 Build e Deploy" + `workflow_dispatch` | **Guardia anti-stale (fingerprint di build).** `deploy.yml` scrive `data/buildinfo.json` (SHA + timestamp del deploy) e `baseof.html` lo emette come `<meta name="pc-build-sha">`/`pc-build-time` su ogni pagina Hugo. Dopo ogni deploy questo workflow campiona ~16 pagine ad alto traffico su Aruba (cache-busted, `no-cache`) con `scripts/verifica-fingerprint-live.sh` e rileva il **drift di generazioni**: se le pagine servono >1 SHA distinto (build diverse convivono), o una pagina non ha la meta (build pre-guardia stantia), o la build piu recente supera 12h (sito congelato), o una pagina e ≠200 → apre/aggiorna **1 issue in-place** (`automazione + urgente`, email al proprietario — **mai Telegram**, canale pubblico) e si **chiude da sola** al rientro. Retry ampi (8×75s) per assorbire la propagazione FTP/cache. Nasce dall'audit 01/07/2026 (chi-siamo di aprile + allerte-meteo di maggio + home di luglio conviventi su Aruba, col semaforo allerta stantio). **Rimedio quando scatta:** fix **mirato** sulle pagine stantie segnalate (cache-bust del sorgente o cancellazione del file lato server — il bump del `state-name` è **VIETATO** dal 03/07/2026: re-upload integrale che non completa mai e congela il sito, vedi rule 05 e il commento in `deploy.yml`). Immune al falso positivo del deploy successivo: un deploy che supera quello triggerante riporta TUTTE le pagine allo stesso SHA nuovo → drift check verde. Sorvegliato dal watchdog universale di `aggiorna-stato-sistema.yml` e mostrato su `/stato-sistema/`. Eseguibile in locale: `bash scripts/verifica-fingerprint-live.sh [--diagnostica]`. |
| `aggiorna-manuale.yml` | Lunedì 06:00 UTC | Confronta hash fonti AGID/Designers Italia/DPC (10 URL), apre issue con checklist tripartita se cambiano: (A) manuale operativo, (B) rules `.claude/` + CLAUDE.md + agent, (C) verifica finale. Regola di coerenza obbligatoria: manuale e rules dicono la stessa cosa |
| `update-bootstrap-italia.yml` | Lunedì 06:00 UTC | Verifica aggiornamenti Bootstrap Italia |
| `audit-sito.yml` | Ogni 6 ore (frequenza alzata con PR #221) | **Audit completo (sezioni)**: contenuti pubblicati (COI, NUE 112, telefono, sede, CAP, placeholder, asset, badge, date, allegati, frasi AGID, draft `_index`, schede stampabili, modalità emergenza, pagine legali, widget) + codice/template (build Hugo, articoli `draft:true`, link a slug inesistenti, sintassi JS, validità YAML workflow, path assoluti template, residui CCV-MB/lombardo/`/index.html`) + governance docs (file presenti, import CLAUDE, badge list coerente, formato date, frontmatter, riferimenti incrociati, pagine obbligatorie, shortcode foto, script export, `dataUltimaRevisione`) + audit aggiuntivo (mixed content `http://`, `image_alt` accessibility WCAG 1.1.1, coerenza dati istituzionali nelle 7 traduzioni, divergenze `hugo.toml` ↔ `data/numeri_utili.yaml`, smoke test rendering H1 pagine critiche, **8 link critici normativa/PDF locali con messaggi dedicati**). **Dal 06/09/2026 sezioni 46-49** (audit esterno): integrità dei file in `static/` (`check-integrita-asset.py`: file vuoti, immagini/ICO/SVG/ZIP/JSON/PDF corrotti — F18/F19), ancore interne e mailto sulla build (`check-ancore.py`), parità schede (`check-parita-schede.py`), tabelle vs dataset (`check-dati-schede.py`); dipendenze `pillow pypdf` installate nel job. Apre/aggiorna **1 issue unica in-place** (titolo stabile senza data, dal 10/06/2026 — prima il titolo datato creava 3-4 issue/giorno) |
| `check-links-sito.yml` | Lunedì 10:00 UTC | Crawl completo del sito con **lychee**: verifica TUTTI i link (interni + esterni, tutte le pagine), apre issue automatica su 404/drift. Catch-all per mantenere aggiornati hub Strumenti, widget, Area Download, link esterni nei contenuti |
| `stale-issues.yml` | Giornaliero 04:00 UTC | **Pulizia issue automatiche**: chiude le issue create dai workflow (`automazione`, `normativa-watcher`, `smoke-test`, `scarica-foto`) dopo 14 gg di inattività (marcatura "stale") + 7 gg ulteriori di silenzio (chiusura). Riapre se qualcuno commenta. **NON tocca** issue umane (label `tracking`, `audit-followup`, `enhancement`, `bug`, `pinned`, `urgente-permanente`). Logica `actions/stale@v9` |
| `normativa-watcher.yml` | Lunedì 06:00 UTC | **Sweep RSS settimanale** delle novità normative PC. Aggrega 37 query Google News (12 tematiche + 25 con filtro `site:dominio.it` per i 25+ siti istituzionali in whitelist: Normattiva, GU, Regione Lazio, DPC, VVF, ISPRA, CNR-IRPI, CMCC, ARPA, ISS, Camera, Governo, Corte Costituzionale, ecc.) + feed RSS TGR Lazio + **DPC home + Area stampa via Firecrawl** (da 19 maggio 2026, attivo solo se secret `FIRECRAWL_API_KEY` è settato; costa ~2 page del tier free Firecrawl per run = ~8 page/mese su 500/mese). Filtra per parole chiave PC, deduplica per URL, apre 1 issue/settimana con digest se trova hit (silenzioso se zero). Logica in `scripts/normativa-watcher.py` (riusabile in locale: `python3 scripts/normativa-watcher.py --days 7`, opzionalmente con `export FIRECRAWL_API_KEY=...` per il modulo DPC). Output JSON archiviato come artifact GitHub Actions per 14 giorni |
| `genera-qr-articoli.yml` | Push su `main` con paths `content/comunicazioni/**.md`, `scripts/genera-qr-articoli.py`, workflow stesso | **Generazione QR articoli come auto-commit**: pip install `segno`, lancia `scripts/genera-qr-articoli.py` (idempotente), se ci sono file nuovi in `static/qr/` (PNG + SVG) li committa con messaggio `[skip-qr] Auto-genera QR articoli mancanti: N nuovi`. **Deploy coalescato** (`deploy-coalescer.yml`) — i QR sono comunque rigenerati inline da `deploy.yml`. Anti-loop: il commit tocca solo `static/qr/**`, fuori dal `paths` filter del workflow stesso. **Pattern doppia rete con `deploy.yml`**: questo workflow è la **fonte di verità per il repo** (mantiene allineamento static/qr/ ↔ content/comunicazioni/); lo step QR omologo dentro `deploy.yml` è il **fail-safe per il sito** (se questo workflow fallisce, deploy genera comunque i QR al volo e il sito non perde il bottone). Storia: 16/05/2026 articolo Giro d'Italia Formia uscito senza bottone "Scarica QR" perché lo script non era in CI e nessuno l'aveva lanciato dopo 5 PR di iterazione da mobile. |
| `scarica-foto-automatica.yml` | Ogni push su `main` | **Step 1 DEPRECATO 2026-05-03** — il vecchio meccanismo "marker `# TODO-foto-*` nel frontmatter → workflow scarica + popola `image:`" è bandito da CLAUDE.md punto 9: il marker veniva renderizzato da Hugo come H1 in produzione + sovrascriveva il banner col titolo. Per foto inline da fonti ufficiali si usa l'agent `pc-image-fixer` (procedura WebFetch + curl + applica-fascia + shortcode `{{< foto >}}`). **Step 2 ATTIVO** — genera cover tipografiche istituzionali banner (gradiente blu + titolo + badge + fascia) per articoli con `image: ""` o con file fisico mancante (via `scripts/auto-cover-mancanti.py`, che chiama `genera-cover.py` + popola frontmatter, mai sovrascrive foto utente). **Step 3** — Commit `[skip-foto-wiki]` se ci sono modifiche, deploy immediato `-f priority=background` (la cover banner non deve restare rotta). |
| `genera-social-bozze.yml` | Ogni push articolo | Genera bozze social (X/Facebook/Instagram/Telegram) + immagini Instagram (post + carosello + story) via Gemini API + Pillow. Skip-loop con `[skip-social]`. Dettagli in `04b-hugo-template-css.md` § "Bozze social automatiche" |
| `indexnow.yml` | Push su `main` con paths `content/comunicazioni/**.md` | **Indicizzazione rapida (IndexNow)**: a ogni articolo nuovo o aggiornato attende il deploy (`sleep 180`) e invia le URL a IndexNow (Bing/Yandex) via `scripts/indexnow-ping.py`, che deduce gli URL degli articoli dall'ultimo commit (esclude i `-facile`) e aggiunge home + archivio. Chiave pubblica servita da `static/<KEY>.txt`. Ping **non bloccante** (un errore non ferma la pipeline). Le notizie vengono indicizzate in minuti invece che in giorni. |
| `promemoria-export-interventi.yml` | Lunedì 07:37 UTC + `workflow_dispatch` | **Promemoria export gestionale interventi (open data)**: legge `periodo.al` da `static/open-data/statistiche-interventi.json`; se più vecchio di **21 giorni** apre/aggiorna 1 issue (label `automazione`) con la procedura completa: scaricare l'export dal gestionale (login umano, non automatizzabile in CI) → `scripts/genera-open-data-interventi.py` se l'export è **cumulativo**, oppure `scripts/aggiorna-open-data-delta.py` se è **parziale** (vedi § "Open data interventi — export parziale") → articoli retrodatati per le giornate di intervento non ancora raccontate (gate `pc-article-reviewer`, niente nomi, orari arrotondati) → PR + merge. La issue si chiude da sola quando il dataset torna fresco. Il file `.xlsx` non va mai committato. Nasce il 17/08/2026 (dataset rimasto fermo al 30/07 con 22 interventi non censiti). |
| `gestione-scadenze.yml` | Lunedì 09:37 UTC | **Sweep articoli scaduti**: scansiona `content/comunicazioni/*.md` per il campo `scadenza:`; se superata e non già archiviato, apre/aggiorna issue con label `automazione + scadenze` con elenco articoli da archiviare (allerte passate, eventi conclusi) o aggiornare. L'agent `pc-content-freshness` può fare audit on-demand più approfondito. |
| `audit-glossario-coverage.yml` | Lunedì 10:07 UTC | **Audit copertura glossario inline**: `scripts/check-glossario-coverage.py` scansiona tutti i .md, estrae sigle PC (regex maiuscole 3+ char, blacklist falsi positivi tipo "NON"/"AND"), confronta con `content/glossario/_index.md`, apre issue con sigle ricorrenti (≥3 occorrenze) non ancora nel glossario. Aggiungerle attiva il popover inline su tutte le pagine che le usano (vedi rule 03 § "Glossario inline con popover"). |
| `controllo-refusi.yml` | Lunedì 10:40 UTC | **Spell-check refusi**: `scripts/check-refusi.py` (hunspell it_IT via spylls + allowlist `scripts/dizionario-pc.txt`) controlla i file **modificati negli ultimi 7 giorni** (articoli `.md` + schede statiche `.html` in `static/formazione/` e `static/giochi/`) e apre/aggiorna 1 issue (label `refusi`) con le parole sospette. Pesca non-parole tipo "cuoperti"→"copriti". Salta sigle MAIUSCOLE e parole con maiuscola iniziale (nomi propri) per ridurre i falsi positivi. Solo i file recenti → issue piccola; per bonificare il legacy o un singolo file on-demand usa l'agent `pc-correttore-bozze` o lo sweep completo `python3 scripts/check-refusi.py`. Write-time: lo stesso script gira sul file nuovo prima del commit (CLAUDE.md § "Automatismo totale"). |
| `controllo-freschezza.yml` | Lunedì 11:07 UTC | **Sweep freschezza contenuti su due estremi**, 1 sola issue combinata `automazione + freschezza`. **(1)** `scripts/check-freshness.py` (solo stdlib) segnala gli articoli `content/comunicazioni/` "anziani" che citano informazioni che invecchiano — telefoni reali, norme (`D.Lgs`/`DPCM`/`L.R.`/`Legge n`/`Decreto`/`Direttiva`), link esterni — con due soglie: **età > 18 mesi + almeno un segnale**, oppure **età > 30 mesi** in ogni caso. **(2)** `scripts/check-articoli-programmati.py` (solo stdlib, agganciato qui dall'audit del 29/05/2026) segnala gli articoli **programmati in uscita nei prossimi 14 giorni** che citano **norme o telefoni** (segnali forti — i link esterni da soli non bastano, già coperti da lychee): contenuto congelato alla stesura che va live "fresco" per data, da riverificare **prima** del deploy. Entrambi escludono `-facile.md` e `archiviato: true`. La riparazione si fa in sessione con `pc-content-freshness` (norme → anche `pc-normative-verifier`). **Volutamente silenzioso** quando non c'è nulla da segnalare. Eseguibili in locale: `python3 scripts/check-freshness.py` e `python3 scripts/check-articoli-programmati.py [--giorni N]` (exit code = n. articoli segnalati). |
| `controllo-fonti-cruscotto.yml` | Lunedì 10:53 UTC | **Smoke-check fonti del cruscotto** (`/cruscotto/`): `scripts/check-fonti-cruscotto.py` (solo stdlib) pinga le 17 fonti dati esterne delle schede (dal 03/09/2026 anche `status.json` del ricevitore SDR OpenWebRX+ IZ0FKE usato dalla vista RADIO della Sala situazioni) — INGV FDSN (terremoti/vulcani), Open-Meteo forecast/air-quality/marine (meteo/aria/mare), DPC radar API, EUMETSAT WMS GeoColour + alta risoluzione (satellite EUMETSAT e satellite ItaliaMeteo, stessa fonte), NASA GIBS (suolo), EFFIS WMS (incendi), ARPA Lazio (aria regionale, con check freschezza Last-Modified), **Copernicus CAMS WMS ECMWF `eccharts.ecmwf.int/wms/?token=public` (aria scala europea, PM/O3/NO2/SO2/dust/pollini)**, **Copernicus EMS Rapid Mapping JSON API `rapidmapping.emergency.copernicus.eu` (attivazioni mappature emergenze EU)**, ItaliaMeteo/MeteoHub WMS (previsioni ICON-2I `meteohub:t2m-t2m` + onde WW3 `meteohub:ww3_hs-hs` + radar SRI `meteohub:radar-sri` su `maps.mistralportal.it`, CC BY 4.0), ItaliaMeteo osservazioni stazioni (API `meteohub.agenziaitaliameteo.it/api/observations`, B12101 CCBY) — e apre/aggiorna 1 issue (label `automazione + cruscotto`) se almeno una è giù. Serve perché un endpoint di terzi che cambia o cade renderebbe la scheda vuota **senza** errore visibile. Non fa deploy. Eseguibile in locale: `python3 scripts/check-fonti-cruscotto.py` (exit code = n. fonti in errore). |
| `audit-versione-facile-coverage.yml` | Lunedì 10:37 UTC | **Audit copertura versione facile (A2)**: `scripts/check-versione-facile-coverage.py` individua gli articoli che — per i criteri di `CLAUDE.md § "Automatismo totale"` + `manuale/parte-25 § 25.7` — dovrebbero avere una versione `<slug>-facile.md` ma ne sono privi. Qualificazione ad alta precisione **sul titolo** (badge `Allerta`/`Emergenza`/`Prevenzione` sempre; `Formazione`/`Informazione` se il titolo cita norme/ISO, categorie vulnerabili o procedure operative; ricorrenze/giornate/Natale escluse). Apre/aggiorna issue `automazione + versione-facile` con i candidati per revisione editoriale (decisione finale umana, possibili falsi positivi). Nasce dopo l'audit del 22/05/2026 in cui l'intera serie ISO era scoperta tranne il pilota. |
| `check-linee-guida-irc.yml` | Lunedì 11:31 UTC | **Controllo Linee Guida RCP 2025 (IRC)**: `scripts/check-linee-guida-irc.py` (solo stdlib) confronta i capitoli PDF pubblicati su `ircouncil.it/linee-guida-rcp-2025/` con la tabella di `/formazione/primo-soccorso/` e apre/aggiorna 1 issue (label `automazione + primo-soccorso`) quando IRC rilascia un capitolo nuovo, ricarica un PDF con URL diverso, o un nostro link sparisce dalla pagina. IRC pubblica le traduzioni progressivamente (al 10/07/2026: disponibili capp. 2-5, 8, 9, 11, 12; attesi 1, 6, 7, 10). Riparazione editoriale in sessione, mai automatica. Eseguibile in locale: `python3 scripts/check-linee-guida-irc.py` (exit code = n. segnalazioni). |
| `check-video-lis.yml` | Lunedì 11:23 UTC | **Check periodico nuovi video LIS** dai 3 canali monitorati: (1) "Io non rischio" (campagna nazionale DPC + ANPAS + INGV + RELUIS + CIMA), (2) **"DPCgov" canale ufficiale del Dipartimento Protezione Civile (PCM)** — non ha playlist LIS dedicata, ma pubblica occasionalmente video istituzionali con interprete LIS, (3) "Abili a Proteggere" (Cooperativa Europe Consulting). Logica in `scripts/check-nuovi-video-lis.py`: confronta i feed RSS dei canali + RSS playlist LIS DPC + RSS playlist "L'attimo decisivo" + scraping HTML del canale Abili a Proteggere con i `youtube_url` già presenti in `data/lis.yaml`. Filtro LIS con word boundary su "LIS" (evita falsi positivi tipo "Elisa") e match "lingua dei segni / lingua italiana dei segni". Se trova nuovi video LIS plausibili, apre/aggiorna issue con label `automazione + lis` e elenco + suggerimento famiglia tematica per ciascun video. Privacy-first: niente embed YouTube nel sito (modalità preferita `lis_section` linka all'hub `/lis/`). |
| `check-video-dpc-eventi.yml` | Bimensile (1° e 15 del mese, cron `47 11 1,15 * *`) | **Check periodico video DPCgov correlabili a contenuti del sito.** `scripts/check-video-dpc-eventi.py` fa scrape di playlist + feed del canale ufficiale DPCgov e cross-match IDF-weighted con titoli/description/corpo degli articoli; apre issue con i video integrabili come "Approfondimenti video". Complementare a `aggiorna-video-correlati.yml` (che rigenera la mappa) e `check-video-lis.yml` (che cerca i video LIS). |
| `aggiorna-video-correlati.yml` | 1° del mese 10:13 UTC | **Re-scraping mensile completo dei canali YouTube monitorati** (al 2026-06-06: **18 canali in lingua italiana**, 9 tematici PC + 9 divulgativi non-tematici — niente canali in lingua straniera per policy editoriale 19 maggio 2026) e **rigenerazione automatica** della mappa pagina→video_correlati. Canali tematici (no filtro): DPCgov, Io non rischio, Abili a Proteggere, INGV terremoti, INGV vulcani, ItaliaMeteo, CIMA Research Foundation, CNSAS, Stefano Solarino (sismologo INGV). Canali divulgativi (con filtro lessicale `DIVULGATIVO_PC_KEYWORDS` ~250 keyword PC in senso ampio): Geopop, NatGeo Italia (edizione italiana), Rai Cultura, Rai News, CICAP, Link4Universe, Wired Italia, CMCC, Dario Bressanini. Logica: `scripts/scrape-catalogo-video.py` via `yt-dlp` (Python, no API key) scarica tutti i video storici in `data/video_dpc_catalogo.yaml` (5000-15000 video); `scripts/genera-video-correlati.py` esegue cross-match IDF-weighted (pesi posizionali title×3, desc×2, body×1, soglia score ≥ 2.0, max 5 video per pagina, vincolo IDF >= 0.7 per i divulgativi non-tematici, include i 59 video LIS già in `data/lis.yaml`) e salva la mappa in `data/video_correlati.yaml`. **🔴 Gate tematico (20/05/2026): un match è valido solo se almeno una parola-ancora è PC-tematica e specifica** (`_anchor_is_topical_specific` su stem da `DIVULGATIVO_PC_KEYWORDS` + sigle `TOPICAL_ANCHOR_SHORT` + luoghi-disastro; i termini PC troppo astratti `TOPICAL_BROAD_STEMS` — crisi/disastro/ricostruzione — non bastano da soli) + denylist `DENY_VIDEO_IDS` per i falsi positivi di ultimo miglio. **Principio permanente: video pertinente o niente sezione** — le pagine senza video davvero pertinente NON hanno la sezione "Approfondimenti video" (meglio nulla che qualcosa di sbagliato; copertura scesa da ~494 a ~123 pagine il 20/05/2026, le pagine non-topiche correttamente senza video). Commit `[skip-aggiorna-video]` se ci sono diff. **Deploy coalescato** (`deploy-coalescer.yml`). **Falsi positivi: i fix vanno nel generatore** (`DENY_VIDEO_IDS` / `STOPWORDS_IT` / `TOPICAL_BROAD_STEMS` / `DIVULGATIVO_PC_KEYWORDS`; dal 01/09/2026 anche la curatela per singola pagina `DENY_PAGE_VIDEO` — falso positivo su una sola pagina — e `FORCE_MATCHES` — abbinamento pertinente verificato a mano che l'IDF non aggancia; procedura in `manuale/parte-32` § 32.8 opzione E), MAI solo nel YAML (rigenerato ogni mese → fix perso). Per aggiungere/rimuovere un canale modificare il dict `CANALI` in `scripts/scrape-catalogo-video.py` (procedura completa in `manuale/parte-32-approfondimenti-video-multicanale.md` § 32.4 e § 32.8). |
| `aggiorna-video-feed.yml` | Lunedì 10:23 UTC + `workflow_dispatch` | **Video recenti dai feed RSS dei canali (complemento settimanale della scansione mensile)**: `scripts/aggiorna-catalogo-video-feed.py` (solo stdlib + PyYAML) legge i feed Atom ufficiali di YouTube dei 18 canali monitorati (ultimi ~15 video ciascuno, ID canale in `CHANNEL_IDS`, nessuna API key né yt-dlp), aggiunge al catalogo `data/video_dpc_catalogo.yaml` solo i video nuovi (stesso formato e stesse chiavi della scansione mensile, che resta la fonte completa) e rigenera la mappa con `genera-video-correlati.py` (stessi filtri e stessa curatela). Commit `[skip-video-feed]` solo se cambia qualcosa; **deploy coalescato**. Fail-safe per canale (feed giù = canale saltato, exit 0). Nasce il 02/09/2026: il video Geopop sul crollo del ghiacciaio in Nepal, uscito poche ore dopo la scansione mensile, era rimasto invisibile al cross-match mentre l'articolo del 27/08 era senza video. Un canale nuovo in `CANALI` va aggiunto anche a `CHANNEL_IDS` (altrimenti l'ID è risolto al volo dalla pagina dell'handle, con avviso nel log). |
| ~~`aggiorna-indice-ricerca.yml`~~ | **RITIRATO 15/07/2026** | L'indice Pagefind non è più committato in `static/pagefind/` (era arrivato a ~432 MB / ~18.000 file versionati — audit esterno 15/07/2026): è un **artefatto di build** generato da `deploy.yml` (`npx pagefind@1.5.2 --site public`) dopo ciascuna delle due build Hugo, sempre allineato ai contenuti del deploy. `static/pagefind/` è in `.gitignore`. In locale, per provare la ricerca con `hugo server`: `bash scripts/genera-indice-ricerca.sh` (genera l'indice locale, non committarlo). |
| `notifica-telegram-articolo.yml` | Push su `main` con paths `content/comunicazioni/**.md` + `workflow_dispatch` | **Notifica Telegram nuovi articoli**: invia un messaggio al canale Telegram del Gruppo solo per gli articoli **aggiunti** (Added) nel commit, non quelli modificati. Logica in `scripts/notifica-telegram-articolo.py` (+ `scripts/telegram_lib.py`). Skip-loop con `[skip-telegram]` nel messaggio di commit. Complementare a `notifica-telegram.yml` (che notifica i cambi di `data/allerta.json`). |
| `notifica-telegram.yml` | Triggerato esplicitamente da `check-allerta.yml` via `gh workflow run` quando `data/allerta.json` cambia + `workflow_dispatch` | **Notifica Telegram allerta/emergenza**: pubblica sul canale Telegram del Gruppo il messaggio di cambio stato (livello allerta, avviso meteo, rischio incendi AIB, modalità emergenza) leggendo il diff di `data/allerta.json`/`data/emergenza.json`. Il trigger esplicito serve perché i push fatti dal `GITHUB_TOKEN` non auto-triggerano i workflow su evento `push` (fix 14 maggio 2026). Complementare a `notifica-telegram-articolo.yml` (nuovi articoli). _(Riga aggiunta il 17/08/2026: il workflow esisteva ma mancava da questa tabella.)_ |
| `genera-pacchetti-kit.yml` | Push su `main` con paths `content/formazione/kit-scuola-*.md`, `static/formazione/schede-stampabili/**`, `scripts/genera-pacchetti-kit.py` + `workflow_dispatch` | **Generazione pacchetti ZIP dei kit scolastici**: quando cambiano i kit per le scuole o le schede stampabili, rigenera gli archivi ZIP scaricabili dei kit per fascia. Idempotente, committa solo se i pacchetti cambiano con `[skip-pacchetti]`. Logica in `scripts/genera-pacchetti-kit.py`. |
| `genera-manuale-pdf.yml` | Push su `main` con paths `content/manuale/**`, layout/CSS del manuale, `scripts/genera-manuale-pdf.py` + `workflow_dispatch` | **Rigenerazione del PDF del Manuale di Protezione Civile** dalla stessa fonte Hugo dei capitoli online: installa WeasyPrint (apt pango/cairo/gdk-pixbuf + pip), build Hugo + serve locale + WeasyPrint sulla pagina `/manuale/versione-stampabile/` (font Titillium dal repo). Committa `static/manuali/manuale-protezione-civile.pdf` se cambiato con `[skip-manuale-pdf]` (push con `pull --rebase` + retry contro non-fast-forward). Fonte unica di verità: reader online e PDF dicono sempre la stessa cosa. |
| `controllo-manuale-normativa.yml` | Mensile (1° del mese) + `workflow_dispatch` | **Controllo delle norme citate nel Manuale** (opera viva): `scripts/check-manuale-normativa.py` (solo stdlib) estrae da `content/manuale/**.md` le citazioni normative (D.Lgs./L./DPCM/D.M./L.R./DGR, UNI/ISO, URN Normattiva) raggruppate per capitolo, e apre/aggiorna 1 issue (label `manuale + normativa`) come **checklist di verifica vigenza**. Promemoria: verificare con l'agent `pc-normative-verifier` su Normattiva/BURL e aggiornare il capitolo se una norma è cambiata/abrogata/sostituita — **flag, non riscrittura automatica** (NO INVENZIONI). Concretizza la regola CLAUDE.md § "Manuale opera viva". |
| `notifica-ci-fallita.yml` | `workflow_run` (completed) sui workflow critici: "⛈️ Aggiornamento Allerta Meteo", "🚀 Build e Deploy", "🚦 Smoke test post-deploy", "🗓️ Pubblicazione programmata articoli" | **Watchdog CI**: se uno di questi workflow **fallisce su `main`**, apre/aggiorna un'issue GitHub (`automazione + urgente`) con link al log, assegnata al proprietario → **notifica via email** (mai Telegram: canale pubblico cittadini). Dedup per titolo (niente spam). Nasce dopo che i fallimenti dell'allerta (apt 404 poppler) erano scoperti solo per caso accedendo a GitHub. Per aggiungere un workflow al monitoraggio: aggiungere il suo `name:` esatto a `on.workflow_run.workflows`. |
| `meteo-lazio.yml` | Orario (cron `8 * * * *`) + `workflow_dispatch` | **Cartina meteo Lazio + riquadro Genzano**: genera ogni ora la cartina meteo regionale con riquadro su Genzano. Dal 31/05/2026 la cartina ha una **cornice di microtesto** (provenienza Gruppo + `protezionecivilegenzano.it` lungo i 4 bordi, `microtext_frame()` in `genera-meteo-lazio.py`, viewBox 642×842) come difesa anti-appropriazione. Commit `[skip-meteo]` solo se l'output cambia. **Deploy coalescato** (`deploy-coalescer.yml`, triggerato anche via `workflow_run` al completamento di questo workflow). **Dall'11/06/2026 le chiamate Open-Meteo hanno retry** (3 tentativi, backoff 15/30s) sugli errori di rete transitori dei runner CI — 4 run consecutivi erano falliti per `Temporary failure in name resolution` lasciando la cartina live stantia. Stesso retry in `genera-meteo-{animazione,sinottica}.py`, `genera-ecmwf-charts.py`, `genera-clima-castelli.py`. |
| `meteo-cartine-extra.yml` | 4×/giorno (cron `23 2,8,14,20 * * *`) + `workflow_dispatch` | **Cartine meteo extra**: genera l'animazione meteo del Lazio (`scripts/genera-meteo-animazione.py`) e la sinottica Italia (`scripts/genera-meteo-sinottica.py`) — quest'ultima è il file `static/images/meteo-sinottica-italia.webp` (asset di workflow, non cover di articolo: il limite 200 KB delle cover non si applica). Commit `[skip-meteo]` solo se cambiano gli output. **Deploy coalescato** (`deploy-coalescer.yml`). |
| `ecmwf-charts.yml` | 4×/giorno (cron `47 3,9,15,21 * * *`) + `workflow_dispatch` | **Carte ECMWF OpenCharts (Meteo Europa)**: `scripts/genera-ecmwf-charts.py` scarica un sottoinsieme curato di carte sinottiche europee a medio termine dall'OpenCharts API pubblica (`charts.ecmwf.int/opencharts-api/v1/`, dati CC BY 4.0, nessuna API key), le converte in WebP e le **auto-ospita** in `static/images/ecmwf/<slug>.webp` (+ metadati `data/ecmwf_charts.json`) per la scheda **"Meteo Europa (ECMWF)"** del cruscotto (`{{< dashboard-ecmwf >}}`). Privacy-first: nessun embed di terzi nel browser. **Le carte ECMWF si rinnovano di fatto 2×/giorno** (corse 00Z e 12Z, disponibili ~08:30 e ~20:30 UTC; 06Z/18Z supplementari): preleviamo 4×/giorno per non mancare un refresh, ma lo script **committa solo se i byte WebP cambiano davvero** (confronto col file esistente) — le corse "a vuoto" non producono deploy. Commit `[skip-ecmwf]`. **Deploy coalescato** (`deploy-coalescer.yml`). Apre issue (label `automazione`) se l'API è irraggiungibile o gli slug prodotto cambiano. Dipendenza: `pillow`. Prodotti: `medium-mslp-wind850`, `medium-2mt-wind30`, `medium-precipitation-type`, `medium-cape-cin` (lista `PRODOTTI` nello script). |
| `clima-castelli.yml` | Mensile (1° del mese, cron `41 4 1 * *`) + `workflow_dispatch` | **Dataset clima Castelli per il Laboratorio meteo** (`/laboratorio-meteo/`): `scripts/genera-clima-castelli.py` (stdlib) scarica le serie storiche giornaliere ERA5 via Open-Meteo (CC BY 4.0) per Genzano, le aggrega in dataset didattici (luglio/anno temperature, pioggia annua, giorni ≥35 °C) e scrive `static/open-data/clima-*.json` + `clima-manifest.json` (forma attesa dal renderer JS). L'ultimo anno solare completo è calcolato in automatico, così gli "esempi pronti" del Laboratorio si estendono da soli. Commit `[skip-clima]` se cambiati. **Deploy coalescato** (`deploy-coalescer.yml`). |
| `comprimi-podcast.yml` | Manuale (`workflow_dispatch`) | **Compressione podcast m4a → 64 kbps mono**: ricodifica via ffmpeg gli episodi podcast ad audio mono 64 kbps (`+faststart`) per tenere i file < 50 MB e il repo più leggero. Commit `[skip-podcast]`, deploy immediato `-f priority=background`. Esecuzione manuale on-demand. |
| `validate-pr.yml` | `pull_request` | **Validazione PR**: esegue il build Hugo target Aruba (`hugo --minify --baseURL "https://www.protezionecivilegenzano.it/"`) e fallisce esplicitamente sui `ERROR` del log (Hugo ritorna 0 anche su WARN). Dal 15/08/2026 include lo step **`scripts/check-jsonld.py`** (eseguibile anche in locale su `public/`): ogni blocco `ld+json` del campione di pagine deve essere JSON parsabile e ogni pagina deve avere il blocco di paternità `copyrightHolder` di `jsonld-copyright.html` — guardia contro il doppio escape (incidente 12/05/2026) e le regressioni del livello di tutela IP. **Dal 06/09/2026** (audit esterno): step **`scripts/check-ancore.py public`** (ogni `#frammento` interno trova il suo id; nessun `mailto:` codificato due volte — F16/F21) e job **`pacchetti-schede`**: rigenera i pacchetti «Stampa tutto» e fallisce se differiscono da quelli committati (F08), poi **`scripts/check-parita-schede.py`** (kit ↔ Stampa tutto ↔ ZIP ↔ cartelle, avvertenze per l'adulto presenti in ogni formato, ZIP apribili offline senza `href="/…"` — F02/F03/F09) e **`scripts/check-dati-schede.py`** (tabelle delle schede uguali ai dataset `/open-data/` citati — F07). Gate di build sulle pull request prima del merge, complementare all'agent `pc-deploy-validator`. |
| `scadenze-conformita.yml` | Lunedì 06:17 UTC + `workflow_dispatch` | **Calendario degli adempimenti legali** (audit 06/09/2026, F14): issue in-place (label `conformita`) se `dichiarazioneAccessibilita` in `hugo.toml` è vuoto, se mancano ≤60 giorni al **23 settembre** (riesame della dichiarazione di accessibilità, AgID) o al **31 marzo** (obiettivi di accessibilità), o se una pagina legale ha `dataUltimaRevisione` più vecchia di 12 mesi (avviso oltre 300 giorni). Si chiude da sola al rientro. La verifica nel merito spetta a `pc-conformita-legale`. |
| `pa11y-ci.yml` | `pull_request` | **Gate accessibilità su PR**: build Hugo (baseURL locale) → serve `public/` su `http://127.0.0.1:8080/` → `pa11y-ci` con **runner axe-core** secondo **WCAG 2.2 AA** su un campione di pagine chiave (home, cosa-fare-adesso, emergenza, allerte-meteo, numeri-utili, contatti, accessibilita, piano-familiare, rischi-prevenzione, comunicazioni, cruscotto). Config in `.pa11yci.json` (root). Intercetta le regressioni WCAG **prima** del deploy — complementare a `lighthouse-audit.yml` (post-deploy) e `validate-pr.yml` (solo build). Per aggiungere/togliere pagine dal campione si edita `.pa11yci.json`; widget click-to-load, iframe e `<dialog>` sono esclusi via `hideElements`. **BLOCCANTE dal 15/08/2026**: rollout report-only concluso (29/05→15/08 con 0 violazioni confermate per l'intero periodo) — una violazione WCAG confermata ora **fa fallire la PR**; gli *incomplete* restano informativi. Il commento del report è pubblicato con `if: always()` così la spiegazione arriva anche (soprattutto) quando il gate è rosso. |

> **Storia merge**: il 26 aprile 2026 sono stati fusi 2 workflow dentro `audit-sito.yml` per consolidare le issue settimanali:
> - `coerenza-docs.yml` (lun 07:00) → sezioni 23-32 (governance docs)
> - `check-normativa-links.yml` (lun 08:00) → sezione 38 (link critici con messaggi dedicati)
>
> Risultato: -2 workflow, 1 issue settimanale invece di 3, stessa copertura.

> **Auto-integrazione approfondimenti (pre-autorizzata):** quando `check-video-lis.yml`, `check-video-dpc-eventi.yml` o `aggiorna-video-correlati.yml` aprono un'issue su un nuovo video — o quando un articolo merita approfondimenti via link ai siti della nostra lista — la sessione Claude integra **da sola, senza chiedere l'OK** (istruzione permanente dell'utente del 20/05/2026) il video/link dove pertinente e pubblica fino a live. Vincolo cogente: **approfondimento pertinente o niente** — se non tratta una tematica del sito NON va allegato (fuori contesto). Specifiche complete in `CLAUDE.md` § "Auto-integrazione di approfondimenti pertinenti (video + link)".

## Key operational notes

- **To activate emergency mode**: set `"attiva": true` in `data/emergenza.json` and fill `tipo`, `titolo`, `descrizione`. Reset to `false` when done.
- **To set weather alert**: edit `data/allerta.json` — change `livello` to `verde/giallo/arancione/rosso`.
- **Draft posts**: set `draft: true` in front matter. They appear locally with `hugo server -D` but are not published. **Regola progetto**: niente articoli in `draft: true` — solo immediato (`date` passata) o calendarizzato (`date` futura).
- **CI deploy**: pushing to `main` triggers `.github/workflows/deploy.yml` which builds twice (once per baseURL) and deploys via FTP to Aruba and via GitHub Pages API. Monitor at the Actions tab.
- **FTP credentials** are stored as GitHub secrets (`FTP_SERVER`, `FTP_USERNAME`, `FTP_PASSWORD`).

## Open data interventi — export parziale: aggiornare per delta, mai sovrascrivere (2 settembre 2026)

🔴 **Regola dell'utente (02/09/2026):** quando l'export del gestionale non è quello completo, **si prende il parziale che arriva e si aggiungono ai dataset pubblicati solo i dati nuovi**. Mai rigenerare da un export filtrato.

I dataset in `static/open-data/` (`statistiche-interventi`, `interventi-per-tipologia`, `automezzi-impiegati`, `statistiche-volontari`, CSV+JSON) nascono da `scripts/genera-open-data-interventi.py`, che **ricalcola tutto** dall'export del gestionale. Funziona solo se l'export è **cumulativo** (dall'adozione del sistema, 4 aprile 2026). Il gestionale però permette di esportare con un filtro di data: se si passa allo script un export filtrato (es. solo l'ultimo mese), i totali pubblicati vengono **sovrascritti con un sottoinsieme** e i mesi precedenti spariscono dal sito. È successo l'01/09/2026 (export 1 agosto–1 settembre: da 126 a 56 interventi), intercettato prima del commit.

**Come si decide quale script usare** — guardare la prima data dell'export:

| Prima riga dell'export | Tipo | Script |
|---|---|---|
| ≤ `periodo.dal` pubblicato (4 aprile 2026) | cumulativo | `python3 scripts/genera-open-data-interventi.py <file.xlsx>` — ricalcola tutto e azzera ogni arrotondamento |
| > `periodo.dal` pubblicato | **parziale** | `python3 scripts/aggiorna-open-data-delta.py <file.xlsx>` — aggiunge il delta ai totali pubblicati |

Entrambi gli script si difendono da soli: il delta rifiuta un export cumulativo (rimanda al generatore), il generatore lo dice nel docstring. `--dry-run` mostra il delta senza scrivere.

**Cosa fa il delta, e cosa non può fare da solo:**

- Riconosce le righe nuove dal **numero di intervento** (`periodo.ultimo_numero` nel JSON, es. `"130/2026"`, scritto da entrambi gli script): funziona anche se l'aggiornamento precedente si è fermato a metà giornata. Sui dataset generati prima del 02/09/2026 usa la data come ripiego.
- Somma con la stessa logica del generatore: interventi, interventi con automezzo, minuti, chilometri, tipologie, uscite per automezzo, presenze dei volontari.
- Gli **indicatori non additivi** li tratta in modo esplicito: gli *automezzi distinti* crescono solo se compare un'etichetta mai vista nell'elenco pubblicato; per i *volontari con almeno un intervento* verifica che chi compare nelle righe nuove risulti attivo anche prima nello stesso export — se qualcuno compare **solo** nelle righe nuove **si ferma** e chiede `--volontari-nuovi N`, perché il pubblicato non contiene nomi e la risposta la sa solo chi conosce il Gruppo. Mai stimare.
- Le **ore** ereditano ±0,1 h di arrotondamento dal totale pubblicato: se ne dà atto nell'output e si azzera al primo export cumulativo.

Dopo lo script, il resto della procedura è invariato: articoli per le giornate di intervento non ancora raccontate (gate `pc-article-reviewer`, niente nomi, orari arrotondati), PR, merge, verifica del deploy. Il file `.xlsx` non si committa mai.

## Snapshot Copernicus EMS — la scheda del cruscotto non legge più l'API dal browser (24/08/2026)

🔴 L'API pubblica `rapidmapping.emergency.copernicus.eu/backend/dashboard-api/public-activations-info/` risponde **200 ma senza header `Access-Control-Allow-Origin`**: dal browser la fetch è bloccata dalla same-origin policy. La scheda `dashboard-ems.html` del cruscotto falliva **in silenzio** (mostrando "Errore caricamento" o l'ultimo dato in `pc-fetch-cache`).

**Soluzione adottata:** il server non ha il vincolo CORS, quindi lo scarico avviene in CI. `scripts/genera-ems-attivazioni.py` gira come step pre-build di `deploy.yml` e salva `static/open-data/ems-attivazioni.json` (schema identico all'API + blocco `_snapshot` con orario e fonte). La scheda legge **prima lo snapshot same-origin**, poi tenta l'API come fallback (se il CORS tornasse), infine `pc-fetch-cache`. Lo script è fail-safe: se l'API è giù **lascia invariato lo snapshot ed esce 0** — mai un deploy rotto per una fonte terza, mai dati azzerati.

`check-fonti-cruscotto.py` continua a pingare l'API a monte: se cade, lo snapshot smette di aggiornarsi (i dati restano, ma invecchiano).

**Stesso pattern per le allerte globali GDACS (25/08/2026):** `scripts/genera-gdacs-eventi.py` gira come step pre-build di `deploy.yml` e salva `static/open-data/gdacs-eventi.json` (eventi correnti GDACS con livello green/orange/red), letto same-origin dalla Sala situazioni `/monitor/` (pannello EMERGENZE — UE e Mondo). Fail-safe identico: API giù = snapshot invariato, exit 0. Fonte a monte pingata da `check-fonti-cruscotto.py` (check "Allerte globali — GDACS").

**Stesso pattern per le notizie delle agenzie (25/08/2026):** `scripts/genera-notizie-agenzie.py` (step pre-build di `deploy.yml`) salva `static/open-data/notizie-agenzie.json` — ultimi 30 titoli da ANSA Top News, ANSA Cronaca e Adnkronos Prima Pagina (solo titolo + link + orario + testata, uso standard dei feed RSS pubblici con attribuzione) — letto same-origin dalla **barra NOTIZIE** della Sala situazioni. Fail-safe per singolo feed (uno giù = si usano gli altri) e globale (tutti giù = snapshot invariato, exit 0). Esiste anche l'endpoint leggero `/comunicazioni/index.json` (ultimi 20 articoli del sito, template `layouts/comunicazioni/list.json`), disponibile come open data per riusi futuri.

**Freschezza degli snapshot (27/08/2026):** oltre agli step pre-build di `deploy.yml`, il workflow dedicato **`aggiorna-dati-sala.yml`** (cron ai minuti 3/18/33/48 di ogni ora) rigenera i tre snapshot (notizie agenzie + GDACS + EMS) e committa `[skip-dati-sala]` solo se i dati sono cambiati. La Sala situazioni legge questi JSON **prima da `raw.githubusercontent.com`** (CORS aperto, host già in `connect-src`): disponibili **al commit, senza aspettare il deploy**, con fallback sulla copia deployata → freschezza effettiva ~15-20 minuti (cron 15 + cache CDN raw ~5). Le fonti con CORS aperto (Open-Meteo, INGV, **USGS** sismi mondiali, **NASA EONET** vulcani attivi, MeteoHub, Blitzortung via WebSocket) restano invece interrogate **direttamente dal browser** della Sala situazioni, con intervalli da 2 a 15 minuti — per queste non serve snapshot.

**Regola generale:** prima di aggiungere al cruscotto una fonte dati letta dal browser, **verifica che esponga il CORS** (`curl -sI -H "Origin: https://www.protezionecivilegenzano.it" <url> | grep -i access-control`). Se non lo espone, la strada è lo snapshot self-hosted generato in CI, non la fetch diretta.
