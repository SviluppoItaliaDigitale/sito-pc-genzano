#!/usr/bin/env bash
# verifica-conteggi.sh — DISATTIVATO (maggio 2026)
#
# Storia: lo script controllava che i conteggi dichiarati sul sito (schede,
# giochi, attività, storie) corrispondessero al numero reale di cartelle.
# Decisione editoriale di maggio 2026: i conteggi sono stati rimossi da tutto
# il sito e dalla documentazione, sostituiti da descrizioni qualitative.
# Non c'è più drift da inseguire perché non ci sono più numeri da allineare.
#
# Lo script è preservato come no-op per non rompere chiamate residue
# (hook locali, workflow CI). Quando avrete eliminato gli ultimi riferimenti,
# il file si può cancellare.

# Esce subito senza fare nulla.
exit 0
