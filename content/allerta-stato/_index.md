---
# Endpoint JSON pubblico dello stato di allerta — alimenta le notifiche
# browser (idea #2 roadmap). NON ha versione HTML: solo /allerta-stato/index.json.
title: "Stato allerta (endpoint JSON)"
outputs:
  - JSON
sitemap:
  disable: true
---
